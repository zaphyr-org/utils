{
    "name": "zaphyr-org/utils",
    "description": "A collection of useful PHP helper classes that you could use every day.",
    "keywords": [
        "utils",
        "helper",
        "array",
        "country",
        "date",
        "file",
        "filter",
        "form",
        "html",
        "math",
        "string",
        "timezone"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "merloxx",
            "email": "merloxx@zaphyr.org",
            "homepage": "https://merloxx.it",
            "role": "Developer"
        }
    ],
    "require": {
        "php": ">=7.2",
        "ext-dom": "*",
        "ext-fileinfo": "*",
        "ext-mbstring": "*"
    },
    "require-dev": {
        "mockery/mockery": "^1.2",
        "phpunit/phpunit": "9.*",
        "phpstan/phpstan-mockery": "^1.0",
        "phpstan/phpstan-phpunit": "^1.0",
        "roave/security-advisories": "dev-master",
        "squizlabs/php_codesniffer": "3.*"
    },
    "autoload": {
        "psr-4": {
            "Zaphyr\\Utils\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Zaphyr\\UtilsTests\\": "tests/"
        }
    },
    "scripts": {
        "cs": "phpcs --standard=PSR12 -n src",
        "cbf": "phpcbf --standard=PSR12 -n src",
        "unit": "phpunit --colors=always",
        "phpstan": "vendor/bin/phpstan analyse --memory-limit 512M",
        "test": [
            "@unit",
            "@phpstan",
            "@cs"
        ]
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
